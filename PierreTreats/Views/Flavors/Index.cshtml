@{
  Layout = "_Layout";
}

@using PierresTreats.Models;

<h1>All Flavors</h1>
<div class="scrollbar">
@if (@Model.Count == 0)
{
    <h3>No Flavors have been added</h3>
}

@foreach (Flavor item in Model)
{
    <li>@Html.ActionLink($"{item.FlavorName}", "Details", new {id = item.FlavorId})</li>
}
<br>
<a href='@Url.Action("Create")'>
    <button type="button" class="button">New Flavor</button>
</a>
<a href='@Url.Action("Index")'>
    <button type="button" class="button">Home</button>
</a>
</div>
